[project]
name = "smart-agriculture-system"
version = "0.1.0"
requires-python = "==3.11.*"
dependencies = [
    "flask",
    "numpy",
    "pandas",
    "scikit-learn",
    "tflite-runtime",
    "pillow",
    "gunicorn"
]
